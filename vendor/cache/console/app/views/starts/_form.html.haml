= semantic_form_for @application, :url => application_start_path(@application), :method => 'put', :html => {:class => 'form'} do |f|
  %p
    Depending on the size of your application it may take up to several minutes to start.
  %p
    Confirm start of application
    %strong
      '#{@application.name}'?

  = f.buttons do
    = link_to 'Cancel', application_path(@application), :class => 'btn start-cancel'
    = f.commit_button 'Start'
    = f.loading
